package com.pcs.event.scanner.listener;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;



public class ScanDaemon {//extends CoreListenerAdapter {

	private static final Logger LOGGER = LoggerFactory.getLogger(ScanDaemon.class);  
	
	/*
	private CoreConsumer consumer;

	public CoreConsumer getConsumer() {
		return consumer;
	}

	public void setConsumer(CoreConsumer consumer) {
		consumer.setMessageListener(this);
		this.consumer = consumer;
	}
	
	@Override
	public void consumeData(List<KafkaStream<byte[], byte[]>> streams) {
		LOGGER.info("Received event stream from kafka {}",streams.size());
		int i=0;
		for(final KafkaStream stream : streams){
			try {
				LOGGER.info("Sending stream **************{} of {}",i,stream.size());
				ExecutorService executor;
				executor = Executors.newFixedThreadPool(10);
				executor.submit(new EventScannerUtil(stream,i));
				LOGGER.info("Sent stream **************{} of {}",i,stream.size());
				i++;
			} catch (Exception e) {
				LOGGER.error("Exception",e);
			}
			
		}
		
	}
	
	public void scan(){
		consumer.listen();
	}*/
	
	
}
