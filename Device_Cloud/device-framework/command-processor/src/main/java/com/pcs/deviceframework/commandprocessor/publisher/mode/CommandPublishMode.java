package com.pcs.deviceframework.commandprocessor.publisher.mode;

import com.pcs.deviceframework.pubsub.enums.PublisherModes;

public enum CommandPublishMode implements PublisherModes {

	COMMAND_PUBLISHER;
}
